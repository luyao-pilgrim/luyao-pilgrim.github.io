# Hooks

## 函数组件和类组件的区别

1.区别及各自优点  
- 类组件本质是OOP，继承，属性，内部状态管理。优点：生命周期更齐全，对于当前单个组件功能更齐全
- 函数组件本质是FP，输入与输出的映射关系。优点：更纯粹，逻辑复用，易于单元测试，没有类组件的this模糊问题，更好的适应未来的时间分片，并发模式

2.如何在合适的业务场景下选择  
- 类组件，在单个复杂组件的生命周期业务逻辑要求更高  
- 函数组件，通用组件，逻辑复用


## 你说学过ahooks库，举例一下印象深刻的  

## hooks内部的原理（为什么不能条件表达式中使用）